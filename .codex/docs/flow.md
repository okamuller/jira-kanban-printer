# 処理フロー

## シーケンス（概要）
1. JIRAがWebhookを送信。
2. 受信APIが署名/スキーマを検証。
3. assignee変更イベントかを判定。
4. 担当者に紐づくプリンタを解決。
5. チケット情報を整形し印刷ドキュメント生成。
6. プリンタキューへジョブ投入。
7. 実行結果を監査ログへ記録。
8. 必要に応じて通知（失敗時）。

## 冪等設計
- キー候補: `jira_event_id + issue_key + assignee + updated_timestamp`
- 既処理判定: `print_jobs`テーブル参照

